{% extends 'base.html.twig' %}


{% block body %}

    <h1> Creation d'un formulaire </h1>
    {{ form_start(formCalendrier) }}

    {{ form_widget(formCalendrier) }}

    <button type="submit" class="btn btn-success">
        {% if textBoutton %}
            Valider les modification
        {% else %}
            Enregistrer
        {% endif %}
    </button>

    {{ form_end(formCalendrier) }}
{% endblock %}